# Frontend: API base URL (no trailing slash). Used when talking to the backend.
# Leave unset or set to http://localhost:8000 for local backend.
# For production backend (e.g. AWS App Runner), set to your backend URL:
# VITE_API_BASE=https://your-backend-url.awsapprunner.com
